//
//  RentoryLocalConfig.swift.example
//  Rentory
//
//  TEMPLATE. Tracked in git — but NOT compiled into the app target
//  (the trailing `.example` suffix keeps Xcode's synchronized file
//  group from treating it as Swift source). This file exists so a
//  fresh clone can see what `RentoryLocalConfig.swift` should look
//  like.
//
//  To use:
//    1. Copy this file to `RentoryLocalConfig.swift` (drop the
//       trailing `.example`).
//    2. Fill in real values for each constant.
//    3. Build. Xcode's synchronized group picks the `.swift` file
//       up automatically — no project-file edit needed.
//
//  `RentoryLocalConfig.swift` is gitignored, so your real values
//  never get committed.
//

import Foundation

enum RentoryLocalConfig {
    /// Inbox that "Send feedback" submissions are addressed to. Used
    /// to build the `mailto:` URL when the user taps "Open in Mail".
    static let feedbackRecipientEmail = "your-email@example.com"
}
